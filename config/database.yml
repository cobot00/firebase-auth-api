default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  url: <%= ENV['DATABASE_URL'] %>

development:
  <<: *default

test:
  <<: *default
  url: <%= ENV["DATABASE_TEST_URL"] %> # CIrcle CIで動かすためにダブルクオートを使用しています

production:
  <<: *default
